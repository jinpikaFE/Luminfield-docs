---
project: Luminfield
graph_namespace: luminfield
type: workspace-index
source_repository: Luminfield
source_branch: main
source_commit: 3ae4faf9e782d247c980356c17ab96133dc6b713
last_verified: 2026-08-20
tags:
  - luminfield/项目
  - luminfield/工作区
---

# 工作区与分支

> [!warning] 状态快照
> 以下内容代表 2026-08-20 17:24 CST 的核对结果。新任务开始前必须重新读取 Git 状态。

## 当前事实来源

- 仓库路径：`/Users/edy/Desktop/personal/Luminfield`
- 分支：`main`
- HEAD：`3ae4faf9e782d247c980356c17ab96133dc6b713`
- 远程仓库：`git@github.com:jinpikaFE/Luminfield.git`
- 跟踪关系：`main` 跟踪 `origin/main`，核对时提交数为 +0 / -0
- Checkout 类型：主工作区 + 3 个本地附加 Worktree
- 工作区状态：主工作区干净；阶段 D 基线已推送到 `origin/main`；三个附加 Worktree 均从 `main@3ae4faf` 创建且当前干净
- [打开仓库](<file:///Users/edy/Desktop/personal/Luminfield/>)
- [中文 README](<file:///Users/edy/Desktop/personal/Luminfield/README.zh-CN.md>)

当前 `main` 与 `origin/main` 均指向阶段 D 基线提交 `3ae4faf`，包含基础钓鱼、第一版鱼类图鉴入口、24 种鱼条件表、图鉴阶段奖励与第一版湿地守潮星灯。后续并行开发使用临时 Worktree/分支隔离，完成后必须合回 `main`、推送主分支，并清理对应 Worktree 和工作分支。

## 当前 Worktree

- 主工作区：`/Users/edy/Desktop/personal/Luminfield`，分支 `main`，HEAD `3ae4faf`，干净。
- 博物捐赠入口：`/Users/edy/Desktop/personal/Luminfield-wt-museum`，分支 `codex/phase-d-museum-donation`，HEAD `3ae4faf`，干净。
- 钓鱼小游戏：`/Users/edy/Desktop/personal/Luminfield-wt-minigame`，分支 `codex/phase-d-fishing-minigame`，HEAD `3ae4faf`，干净。
- 24 鱼图标：`/Users/edy/Desktop/personal/Luminfield-wt-fish-icons`，分支 `codex/phase-d-fish-icons`，HEAD `3ae4faf`，干净。
- 临时 work 分支不作为交付分支；完成、验证、合回 `main` 并推送主分支后，删除对应 worktree 和本地/远端临时分支。

## 本地工具链

- 工具目录：`/Users/edy/.codex/tools/luminfield/`
- Godot：`godot/Godot_mono.app`
- .NET：`dotnet/dotnet`
- 项目目标框架：`net8.0`
- SDK 固定版本：10.0.302
- Finder 启动器：`/Users/edy/.codex/tools/luminfield/Luminfield Godot.app`
- 仓库内启动入口：`scripts/open_editor_macos.command`

直接在 Finder 中打开原始 `Godot_mono.app` 时，Finder 环境没有项目私有 SDK 的 `DOTNET_ROOT`，会出现 “Failed to load .NET runtime / hostfxr” 提示。使用上述 Finder 启动器或仓库内 `.command` 启动；修复没有安装或修改全局 .NET。

常用验证：

```bash
export DOTNET_ROOT=/Users/edy/.codex/tools/luminfield/dotnet
export PATH="$DOTNET_ROOT:$PATH"

dotnet build Luminfield.sln --configuration Debug
dotnet test tests/Luminfield.Tests/Luminfield.Tests.csproj --configuration Debug
/Users/edy/.codex/tools/luminfield/godot/Godot_mono.app/Contents/MacOS/Godot \
  --headless --editor --path . --quit-after 8
```

三平台导出：

```bash
./scripts/export_all.sh
```

## 产物位置

- macOS：`builds/macos/Luminfield.zip`
- Windows：`builds/windows/Luminfield.exe`
- Linux：`builds/linux/Luminfield.x86_64`
- 视觉验收截图：`artifacts/screenshots/`

`builds/` 和 Godot/.NET 中间产物不作为项目知识来源。

## 使用规则

1. 新任务先执行仓库、分支、HEAD 和短状态检查。
2. 记录工作时核对真实 HEAD，并明确区分提交基线与未提交修改。
3. 任何提交、远程配置和推送行为都需要用户明确授权。
4. 并行开发继续使用一任务一 Worktree，并在独立集成 Worktree 通过组合验证后再更新 `main`；每轮完成、集成且核对干净后立即清理本轮 Worktree，默认保留分支与提交，除非用户明确要求保留 Worktree 或删除分支。
5. 已验证变更同步到 `变更记录/`；诊断但未实施的内容不得写成已完成。
