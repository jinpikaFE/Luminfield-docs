---
project: Luminfield
graph_namespace: luminfield
type: workspace-index
source_repository: Luminfield
source_branch: main
source_commit: 52e6f8a20656a9ccd994e174e98b0135750ec240
last_verified: 2026-08-21
tags:
  - luminfield/项目
  - luminfield/工作区
---

# 工作区与分支

> [!warning] 状态快照
> 以下内容代表 2026-08-21 的核对结果。新任务开始前必须重新读取 Git 状态。

## 当前事实来源

- 仓库路径：`/Users/lipengpeng/Luminfield`
- 分支：`main`
- HEAD：`52e6f8a20656a9ccd994e174e98b0135750ec240`
- 远程仓库：`git@github.com:jinpikaFE/Luminfield.git`
- 跟踪关系：`main` 跟踪 `origin/main`，核对时本地领先 1、落后 10
- Checkout 类型：仅主工作区，无附加 Worktree
- 工作区状态：无附加 Worktree；游戏仓库干净。阶段 F 当前暂停快照已本地提交；普通推送因远端非快进被拒绝，未强推、未合并或变基
- [打开仓库](<file:///Users/lipengpeng/Luminfield/>)
- [中文 README](<file:///Users/lipengpeng/Luminfield/README.zh-CN.md>)

当前本地 `main@52e6f8a` 是阶段 F 暂停快照，`origin/main@c5d0687` 另有 10 个本地尚未整合的远端提交，两侧存在大量同名文件重叠。游戏仓库普通推送已被 GitHub 非快进拒绝；在用户选择“合并主线”或“推送快照分支”前，不拉取整合、不强推，也不覆盖远端。

## 当前 Worktree

- 无附加 Worktree；`git worktree list` 仅包含主工作区 `main@52e6f8a`。
- 31 个 `codex/*` 本地分支和提交仍保留，可按需重新创建 Worktree。

## 本地工具链

- 项目目标框架：`net8.0`
- SDK 固定版本：10.0.302
- 仓库内启动入口：`scripts/open_editor_macos.command`

当前机器已在 `/Users/lipengpeng/.codex/tools/luminfield` 安装并验证项目私有工具链：.NET SDK 10.0.302、Microsoft.NETCore.App 8.0.30 runtime 和 Godot 4.7.1 stable mono。`README*`、`export_all.sh` 与 `open_editor_macos.sh` 已改为当前用户与 `$HOME` 可解析路径，不再沿用历史 `/Users/edy/...` 绝对路径。

常用验证：

```bash
/Users/lipengpeng/.codex/tools/luminfield/dotnet/dotnet build Luminfield.sln
/Users/lipengpeng/.codex/tools/luminfield/dotnet/dotnet test tests/Luminfield.Tests/Luminfield.Tests.csproj
LUMINFIELD_TOOLS=/Users/lipengpeng/.codex/tools/luminfield ./scripts/open_editor_macos.sh
```

三平台导出：

```bash
./scripts/export_all.sh
```

## 产物位置

- macOS：`builds/macos/Luminfield.zip`
- Windows：`builds/windows/Luminfield.exe`
- Linux：`builds/linux/Luminfield.x86_64`
- 视觉验收截图：`artifacts/screenshots/`

`builds/` 和 Godot/.NET 中间产物不作为项目知识来源。

## 使用规则

1. 新任务先执行仓库、分支、HEAD 和短状态检查。
2. 记录工作时核对真实 HEAD，并明确区分提交基线与未提交修改。
3. 任何提交、远程配置和推送行为都需要用户明确授权。
4. 并行开发继续使用一任务一 Worktree，并在独立集成 Worktree 通过组合验证后再更新 `main`；每轮完成、集成且核对干净后立即清理本轮 Worktree，默认保留分支与提交，除非用户明确要求保留 Worktree 或删除分支。
5. 已验证变更同步到 `变更记录/`；诊断但未实施的内容不得写成已完成。

## Git 自动状态

<!-- codex:git-status:start -->
> [!info] Git 自动状态
> 核验日期：2026-08-21。数据来自本地 Git；未执行 fetch，远端引用表示本地已获取的最新值。

| Checkout | 分支 / HEAD | 对比基线 | 工作区状态 |
| --- | --- | --- | --- |
| `/Users/lipengpeng/Luminfield` | main / `52e6f8a2` | 落后 10、领先 1 | 干净 |

基线引用：`origin/main`。dirty 仅记录数量；具体文件始终以对应 checkout 的实时 `git status --short` 为准。

### 已记录的 Git 引用

| Ref | HEAD |
| --- | --- |
| `origin/main` | `c5d06872` |
<!-- codex:git-status:end -->
