---
project: Luminfield
graph_namespace: luminfield
type: module
module: 生成素材与图集管线
source_branch: main
source_commit: 52e6f8a20656a9ccd994e174e98b0135750ec240
last_verified: 2026-08-21
status: active
tags:
  - luminfield/项目
  - luminfield/模块/素材
---

# 生成素材与图集管线

## 原则

- 新增地图实体、人物、作物、物品、设施、天气表现或独立 UI 图标前，先盘点现有图集。
- 缺少合适位图时，使用内置图像生成制作原创素材；临时色块、几何图形或字符图标不是完成态。
- 素材只表达视觉，碰撞、成熟、天气、人物身份、机器状态、任务和存档仍由 Core 驱动。

## 生产流程

1. 明确用途、运行时尺寸、单元尺寸、图集行列、观察角度、落地基线、透明/色键方式和禁止项。
2. 复用现有暮色魔法农场调色板与最近邻像素密度。
3. 需要透明背景时先生成纯色色键源图，通常保存为 `*_chroma.png`。
4. 使用本地去色键流程生成透明运行时 PNG；源图和最终图都保存在 `assets/generated/`。
5. 检查裁切边界、alpha、像素密度、落地基线、近红/品红残留和最近邻缩放。
6. 通过 `GeneratedArt.cs` 或对应 View 使用稳定 ID 与确定性图集索引接入。
7. 在 `assets/generated/README.md` 记录用途、布局、提示词摘要、处理参数和接入位置。
8. 在 macOS 真实 640×360 画面检查比例、轮廓、遮挡和场景一致性。

## 文件职责

| 位置 | 职责 |
| --- | --- |
| `assets/generated/*_chroma.png` | 可追溯的色键生成源图 |
| `assets/generated/*.png` | 透明或稳定裁切后的运行时素材 |
| `assets/generated/README.md` | 布局、提示、处理和接入记录 |
| `src/Game/GeneratedArt.cs` | 加载、裁切和图集映射 |
| 各 `*View.cs` | 按领域 ID 投影到地图或 UI |

`orchard_hives.png` 是阶段 C 果树与蜂巢运行时图集，提供月梅树苗、成熟月梅树、辉巢和星蜜图标；业务状态仍由 `OrchardSystem` 与稳定 ID 驱动。

`rainveil_crops_chroma.png` 与 `rainveil_crops.png` 是阶段 F 雨幕季农业纵切的生成源图与透明运行时图集。图集为 1536×1024、6 列×4 行、单元 256×256；四行依次是涟光菇、潮晶芋、灯穗苇与雨幕莲，六列依次是种子袋、收获物与四个成长阶段。本地去红键、防溢色与确定性重排后，alpha 只含全透明/全不透明，可见近色键像素为 0，主体最大 220×218，最小水平安全边距 18 像素。作物成熟、灯穗苇复生、品质、价格和存档状态仍由 Core 驱动。

`farm_rainveil_backdrop.png` 是 1536×1024、不透明的雨幕季家园季相。它以内置图像生成编辑模式保留原背景的小屋门、温室、六块农田、广场、路径、池塘、码头、瀑布、围栏和南出口注册，只改变湿润地表、雨后植被、冷青紫雾气与灯光；第二次精确编辑移除所有空中落雨线，避免把季节与天气合并。运行时以 0.5 最近邻缩放，碰撞、农田范围、日期与天气仍由代码驱动。

`starharvest_crops_chroma.png` 与 `starharvest_crops.png` 是阶段 F 星熟季农业纵切的生成源图与透明运行时图集。图集为 1536×1024、6 列×4 行、单元 256×256；冻结行序为鎏金芽、日藏瓜、冠星藏红花和琥珀缕果，六列依次是种子袋、收获物与四个成长阶段。定向修图把鎏金芽从谷穗轮廓改为连续金色卷芽；双阶段去红键和按真实连通主体重注册后，alpha 只含全透明/全不透明，可见近色键像素为 0，主体不超过 220×218，最小水平安全边 18 像素，基线统一为 `y=232`。成熟、三档品质、琥珀缕果三夜复生、价格和存档仍由 Core 驱动。

`farm_starharvest_backdrop.png` 是 1536×1024、不透明 RGB 的星熟季家园季相。它以内置图像生成编辑模式保留原背景的小屋门、温室、六块农田、广场、道路、池塘、码头、瀑布、围栏和南出口注册，只把植被、地表和暮色光照调整为暖金、琥珀、李紫和青绿的成熟季视觉；素材不含静态雨线、风粒子、人物、动物、节庆摊位、文字或新增交互物。运行时以 0.5 最近邻缩放，碰撞、农田范围、绝对日期与天气仍由代码驱动。

`exploration_props.png`、`exploration_props_rainveil.png`、`exploration_props_starharvest.png` 与 `exploration_props_longnight.png` 是 1254×1254、严格 4×4 的基础/雨幕/星熟/长夜大世界透明运行图集；后三季另保留品红 RGB 色键源图。16 个索引顺序沿用既有树、柳树、晶体、岩石、灌木、蘑菇、拱门、石柱、神龛、水井、桥、路牌、宝箱、花、芦苇与遗迹柱，四图逐格 alpha 包络同注册。运行图 alpha 仅 0/255、四角透明；季相只改材质、叶色、湿润/成熟/霜封细节，资源位置、碰撞、耗尽、天气和存档由既有代码负责。

`gleamrise_planting_festival_backdrop.png` 是 1536×1024、不透明 RGB 的芽辉播种祭独立花庭，运行时裁切 `(0,80,1536,864)` 到 640×360；背景只保留北侧仪式台、南侧出口和运行时设施空区，不烘焙 NPC、共享花田状态、交换摊或天气。`gleamrise_planting_festival_props_chroma.png` / `.png` 是 1254×1254、2×2 的纯品红源图与透明运行图，四格为空田、同位萌芽、同位繁花和四种子活动桌；二值 alpha、0 个近品红残留，局部边界为 `(43,165,583,590)`、`(43,165,583,590)`、`(43,165,583,590)`、`(88,242,538,590)`。进行中只使用空田底图并叠加真实已播稳定种子图标，完成态才切换繁花；计时、评分、奖项、花辉币和存档不由素材承载。

`longnight_lantern_feast_backdrop.png` 是 1536×1024、不透明 RGB 的长夜灯宴独立场景，沿用星熟集市场景的南门、北台和三处设施注册，只改变为霜蓝长夜、月牙灯阵与暖金灯火，不烘焙 NPC、设施、料理、礼物或天气。`longnight_lantern_feast_props_chroma.png` / `.png` 是 1254×1254、2×2 的品红源图与透明运行图，四格为共辉宴桌、礼物架、休眠星仪与同位点亮星仪；二值 alpha、透明四角、0 个近品红残留，局部边界固定为 `(43,297,583,590)`、`(112,100,514,590)`、`(85,217,542,590)`、`(85,217,542,590)`。完成态料理、礼物和回礼继续叠加真实稳定物品图标；日期、仪式、奖励、灯愿结与区域星灯状态不由素材承载。

`farm_longnight_backdrop.png` 是 1536×1024、不透明 RGB 的长夜季家园季相。它以内置图像生成编辑模式保留原背景的建筑门、六块农田、广场、道路、水域、码头、瀑布、围栏和南出口注册，只加入静态薄霜、克制积雪、冷蓝紫植被与长夜月光；素材不含空中落雪、天气粒子、人物、动物、作物、文字、UI 或业务状态。尺寸、sRGB 三通道与无 alpha 已检查，运行时以 0.5 最近邻缩放，户外封土和温室例外由 Core 驱动。

`longnight_snow_weather_chroma.png` / `longnight_snow_weather.png` 是 1536×1024、4×2 的长夜雪色键源图与透明运行时图集。第一行是四种远近雪晶，第二行前三格是阵风三帧，右下是 HUD 图标；实际色键 `#fa03e5` 以边缘自动取色、容差 75、去溢色和 1 像素收边处理。最终 alpha 仅 0/255，1,361,700 像素透明、211,164 像素不透明，8 个主体均留在 384×512 单元内。运行时只裁切真实 alpha 边界并最近邻绘制，天气日期、户外减速、室内例外和存档不由图集承载。

`homestead_starlight_pedestal_chroma.png` / `homestead_starlight_pedestal.png` 是 1254×1254、2×2 的家园丰穰星灯色键源图与透明运行时图集。顶行休眠/复苏台座重注册到相同 `y=590` 基线，局部 alpha 边界分别同为 `(57,70,569,590)`；底行是三节点封印和八方灌溉辉印。自动取键、容差 65、去溢色与 1 像素收边后 alpha 仅 0/255，近品红残留为 0；地图以 78×78、UI 以 44×40/36×36 最近邻显示，提交和奖励状态不由图集承载。

`meadow_starlight_pedestal_chroma.png` / `meadow_starlight_pedestal.png` 是 1254×1254、2×2 的草甸和鸣星灯纯品红色键源图与透明运行时图集。顶行休眠/复苏花萼台座具有完全相同的局部 alpha 边界 `(105,208,522,590)`；底行三节点封印与远香授粉辉印边界为 `(95,199,531,428)`、`(127,213,499,414)`。运行图 alpha 仅 0/255，四格独立且 0 个可见近品红像素；地图以 78×78、UI 以 44×40/36×36 最近邻显示，物品、节庆结果、授粉距离和存档状态不由图集承载。

`cottage_second_upgrade_interior.png` 是 1536×1024 RGB 的第二次小屋升级内景，只把第一次升级图右下预留区编辑并局部羽化合成为晶火灶台与封闭食材柜，房壳/出口/床/书架/壁炉/通路注册保持不变。`cottage_kitchen_assets_chroma.png` / `.png` 是 1536×1024、4×2 的纯品红源图与二值 alpha 运行图，依次承载项目、厨房、pantry、四料理和料理分类图标；八格均在冻结安全边内，0 个距 `#ff00ff` 100 以内可见像素，规则与存档不由位图承载。

`village_npcs_wave_2_chroma.png` / `village_npcs_wave_2.png` 是阶段 F 首批 NPC 扩容的 1024×1024、4×4 四方向人物图集。四行稳定映射为 `halden`、`mavea`、`sivren`、`dorrik`，四列为下、上、左、右；色键源图为 RGB，运行图为 RGBA 二值 alpha，透明四角、0 个距 `#ff00ff` 65 以内可见像素。16 帧局部 alpha 高度均为 218、底部边界统一为 `y=238`，水平安全边不少于 62 像素。运行时通过 `NpcArtCatalog` 的稳定 NPC ID 映射解析，不把日程顺序或存档状态写进图集。

`village_npcs_wave_3_chroma.png` / `village_npcs_wave_3.png` 是阶段 F 达到 16 人下限的 1024×1024、4×4 四方向人物图集。四行稳定映射为 `yvara`、`brial`、`pavri`、`roven`，四列为下、上、左、右；纯 `#ff00ff` RGB 色键源图经逐单元去键与确定性重注册得到 RGBA 运行图。16 帧均非空、alpha 仅 0/255、透明四角、0 个可见近品红像素，局部顶部/底部统一为 `y=20/238`。运行时只按 `NpcArtCatalog` 的稳定 NPC ID 与 `village_npcs_wave_3` 图集 ID 解析，人物关系、日程、节日锚点和存档状态不由图集承载。

`crystal_grotto_survey_interior.png` 是 1536×1024、不透明 RGB 的固定五室辉晶地窟内景，运行时仍裁切 `(0,80,1536,864)` 到 640×360；只画空矿槽、封脉壁龛、升级台和第 5 室空锚座。`crystal_grotto_assets_chroma.png` / `.png` 是 1024×768、严格 4×3 的色键源图与透明运行图，前两行为四类矿脉/背包图标，第三行为入口、封脉、深度锚和青铜星纹铲图标。`crystal_vale_starlight_pedestal_chroma.png` / `.png` 是 1254×1254、严格 2×2 的晶谷星灯双态与节点/奖励图标。两张运行图 alpha 仅 0/255、四角透明、0 个可见近 `#ff00ff` 像素；工具等级、耗尽、勘探进度和星灯完成态均由 Core 驱动。

`starfall_ruins_trial_interior.png` 是 1536×1024、不透明 RGB 的固定三室遗迹试炼内景；`starfall_ruins_combat_chroma.png` / `.png` 是 2048×1024、严格 8×4 的三类敌人四方向静立/移动帧与短刃、斩击、弹体、预警、生命和闪避图集；`starfall_ruins_artifacts_chroma.png` / `.png` 是 1024×768、严格 4×3 的四件遗物世界态/图标与遗迹门/档案馆陈列双态；`starfall_ruins_starlight_pedestal_chroma.png` / `.png` 是 1254×1254、严格 2×2 的第六灯双态、节点与六光汇聚图。运行图使用稳定 ID 裁切，敌人/遗物/战斗/捐赠/星灯状态全部由 Core 驱动。

`starfall_sixfold_gate_chroma.png` / `starfall_sixfold_gate.png` 是 1254×1254、严格 2×2 的后续星门准备素材，四格为六槽空基座、施工态、休眠门和六光点亮门。色键源图四角为纯 `#ff00ff`，透明图四格非空、alpha 仅 0/255、四角透明，已目检裁切与像素密度；当前没有接入 `GeneratedArt`、View 或 Core，不代表星门施工、交互、传送或存档已经实现。

`homestead_greenhouse_portal_chroma.png` 与 `homestead_greenhouse_portal.png` 是 1254×1254、严格 2×2 的月露温室门面源图与透明运行时图集；四格为封闭、施工、完成和项目图标。品红去键与确定性重注册后 alpha 仅含 0/255，三个地图主体统一落到每格 `y=590` 基线，可见近色键残留为 0；运行时以 0.10 最近邻覆盖既有家园温室门，业务阶段只读取稳定项目 ID `homestead_greenhouse`。

`homestead_greenhouse_interior.png` 是 1536×1024、不透明 RGB 温室内景，稳定裁切 `(0,80,1536,864)` 到 640×360。四块空床分别注册到四组 3×2 Core 格，左下蓄水池和底部出口与 `GreenhouseLayout` 对齐；背景不烘焙作物、耕地、湿润、天气或施工状态。

`homestead_starfeather_coop_chroma.png` / `homestead_starfeather_coop.png` 是 1254×1254、2×2 的星羽鸡舍门面源图与透明运行时图集，四格为未承建、施工中、完成态和项目图标；前三态重注册到 `y=590`，运行图 alpha 仅 0/255 且近品红残留为 0。`starfeather_coop_interior.png` 是 1536×1024 不透明内景，只烘焙空饲槽、空巢箱、栖架与通路。

`starfeather_chicken_chroma.png` / `starfeather_chicken.png` 是 2048×768、8×3 的动物源图与透明运行时图集；前两行分别为幼年/成年四方向静立/迈步，第三行是草甸饲料、星羽蛋、三态心情、已照料、产物就绪和放牧图标。16 个动物帧脚底统一 `y=232`，24 格均独立，alpha 仅 0/255 且近品红残留为 0；年龄、方向、心情、产蛋和路线不由图集承载。

`livestock_automation_console_chroma.png` / `livestock_automation_console.png` 是 1254×1254、2×2 的星织牧务中枢色键源图与透明运行时图集；四格为锁定、启用、饲料与产物管理状态。四个主体按真实 alpha 重注册到 cell-local `(90,105,538,590)`（第三格右边界 537），运行图 alpha 仅 0/255 且近品红残留为 0；地图最近邻约 46×49，施工完成、28/12 容量与缓冲内容仍由稳定项目和 `AnimalSystem` 决定。

## 稳定接入

- 图集行列、裁切矩形和角色方向顺序必须明确记录。
- 运行时索引由稳定业务 ID 映射，不能依赖字典偶然顺序。
- 生成背景不能承担碰撞；碰撞来自 `WorldDefinition`、布局或对应 Core 定义。
- 生成建筑内景不能单独决定入口、出口、开放时间或设施能力。
- 替换图集时不得改变既有稳定 ID 或存档状态。

## 完成标准

- 源图与运行时图均在仓库中。
- `assets/generated/README.md` 已同步。
- alpha 与色键残留检查通过。
- 单元裁切、落地基线和最近邻缩放经过实际检查。
- 对应领域状态、预览、碰撞和本地化测试仍通过。
- macOS 实机画面已目检；未在 Windows/Linux 实机运行时不能声称跨平台视觉通过。

2026-08-20 的雨幕季作物图集已在深色背景、34×34 作物尺寸、28×28 种子/收获图标尺寸与 640×360 macOS Metal 真实运行画面中检查，截图为 `100-rainveil-crops-zh.png`。同日雨幕季背景已在晴朗天气下检查六块农田、设施、路径、池塘与南出口注册，截图为 `110-rainveil-homestead-clear-zh.png`；芽辉季原背景回归截图为 `111-gleamrise-homestead-regression-zh.png`。星熟季图集也已检查 24 单元 alpha/色键/安全边、34×34 成熟轮廓、28×28 收获图标和 Apple M5 / Metal 实机画面，截图为 `120-starharvest-crops-zh.png`。星熟季背景已在晴朗天气下目检暖金成熟季相、六块农田、设施、路径和池塘注册，截图为 `130-starharvest-homestead-clear-zh.png`；雨幕季与芽辉季背景回归截图为 131–132。长夜背景已在截图 `160-longnight-homestead-blocked-zh.png` 中目检霜雪、六块农田、设施和水域注册，商铺截图 `161-longnight-emporium-week1-zh.png` 同时验证温室限定说明与四项库存布局。长夜雪图集已在截图 170–173 中检查前瞻、户外远中近雪晶与阵风、真实目标提示分层、温室无雪和同季晴天清场。家园丰穰星灯图集已在截图 180–184 中检查休眠/复苏同位切态、78×78 真实轮廓、暖金错误工具态和中英面板图标/布局。星羽鸡舍/动物素材已在截图 200–205 中检查三态门面、幼/成年轮廓、晴日放牧、长夜留舍、28×28 物品图标、真实目标高亮和英文巢箱容量阻塞。月绒羊/露角兽素材已在截图 210–221 中检查双动物投影与两类真实产物站；牧务中枢素材已在截图 230–233 中检查 46×49 控制台轮廓、中英管理面板和六项目施工布局；芽辉播种祭背景/图集已在截图 240–245 中检查独立花庭、空/部分/完成花田状态、活动桌、交换摊、真实播种格轮廓及中英文 2×2 种子选择布局；长夜灯宴背景/图集已在截图 270–276 中检查独立霜蓝广场、宴桌/礼物架/星仪双态、真实料理与回礼叠加、中英文面板和暖金错误工具轮廓。

草甸和鸣星灯图集已在截图 250–254 中检查休眠/复苏双状态同位、真实 78×78 轮廓、中英文三节点图标与布局，以及远香授粉下相距 6 格的辉巢/月梅投影；五张截图均为内部 640×360 的严格 2× Apple M5 / Metal 捕获。

第二次小屋升级与厨房素材已在截图 260–266 中检查七项目施工面板、旧预留区、完工厨房/食材柜、青绿/暖金真实对象轮廓、四配方中文面板、pantry 物品名/数量与英文随身料理布局；七张均为内部 640×360 的严格 2× Apple M5 / Metal 捕获。

四名新人图集已在截图 280–286 中检查世界/档案馆投影、三个主节日的 12 人布局、英文对话和暖金错误工具轮廓；四方向身份、人物落地、最近邻缩放与 HUD/设施遮挡均以 Apple M5 / Metal 实画确认。

雨幕/星熟大世界季相图集已在截图 310–314 中检查基础日 14、雨幕日 15、星熟日 29 的同注册全景，以及雨幕小雨下树木青绿可执行轮廓、星熟星尘风下水晶暖金错误工具轮廓；五张均为内部 640×360 的严格 2× Apple M5 / Metal 捕获。

四季采集图集使用 `seasonal_forage_chroma.png` / `seasonal_forage.png` 的 1024×1024、严格 4×4 合同：列为芽辉/雨幕/星熟/长夜，第一/二行是林地实体与图标，第三/四行是草甸实体与图标。内置图像生成的 1254×1254 原始输出先最近邻规范到合同尺寸，再生成纯 `#ff00ff` RGB 原稿与二值 RGBA 运行图；16 格均非空且各只有一个连通主体，水平安全边至少 18px、顶部至少 20px、底部不超过单元 `y=232`。截图 340–345 已检查地图实体、错误工具、星尘风地图标记和中英文图鉴。

辉晶地窟与晶谷星灯素材已在截图 360–366 中检查矿脉、升级、深室、图鉴和第五灯；星落遗迹四组运行时素材已在截图 367–372 中检查入口、敌人移动/攻击、遗物取得、档案馆捐赠与第六灯双态。长夜探索图集已在截图 373 中检查六区季相。六辉星门图集只完成 1254×1254 RGB/RGBA、四格非空、二值 alpha 和透明/品红四角检查，未进行运行时截图，因为当前版本尚未接入。

## 关联

- [[Luminfield/模块/世界、场景与建筑|世界、场景与建筑]]
- [[Luminfield/模块/UI、本地化与输入|UI、本地化与输入]]
- [[Luminfield/模块/测试、试玩与发布验证|测试、试玩与发布验证]]
